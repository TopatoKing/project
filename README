For dev server:

    1. clone the github repo

    2. download node.js

    3. run `npm i`

    4. run `npn run dev`

To interact with the database, open a new terminal and run: `npx prisma studio`
This command will open the Prisma Studio in your browser. 
You can create, read, update, and delete records in the database using this tool.

You can go directly to the live website by clicking this link: https://mycakeyworld.vercel.app/