- Create a .env.local file in the root of the project





In the .env folder add the lines:
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

Open a new terminal and run the following command: npx prisma studio
This command will open the Prisma Studio in your browser. 
You can use this tool to interact with the database. 
You can create, read, update, and delete records in the database using this tool.

Open another terminal and run the following command: npm run dev
This command will start the development server.
This is how you can navigate the website whilst running is locally.
alternatively you can go directly to the live website by clicking this link: https://mycakeyworld.vercel.app/